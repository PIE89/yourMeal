export const getBurgers = (state) => state.burgers.burgers;
export const getBurgersErrors = (state) => state.burgers.errors;
export const getBurgersLoading = (state) => state.burgers.isLoading;
export const getBurgersPage = (state) => state.burgers.page;
export const getBurgersLimit = (state) => state.burgers.limit;
export const getBurgersHasMore = (state) => state.burgers.hasMore;
